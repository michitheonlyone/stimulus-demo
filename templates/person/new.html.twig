{% extends 'base.html.twig' %}

{% block title %}New Person{% endblock %}

{% block body %}
    <h1>Create new Person</h1>
    <a href="{{ path('app_person_index') }}">back to list</a>
    <hr />

    {{ include('person/_form.html.twig') }}
    <button class="btn btn-success">{{ button_label|default('Savedoes not work!') }}</button>

{% endblock %}
